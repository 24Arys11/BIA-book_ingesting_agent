# BIA-book_ingesting_agent
BIA (Book Ingesting Agent) is a python implementation of a Langgraph Agent that should take as input a book and an instructions.txt file and output some files depending on the instructions (eg. `book_summary.md`)
